spring:
  application:
    name: inventory-service
  datasource:
    url: jdbc:h2:mem:books
    driver-class-name: org.h2.Driver
    username: sa
    password: password
    jpa:
      database-platform: org.hibernate.dialect.H2Dialect
      hibernate:
        ddl-auto: create
      show-sql: true
  h2:
    console:
      enabled: true
      path: /h2-console
  liquibase:
    change-log: db/changelog/changelog.yml

server:
  port: 8080

inventory:
  contact:
    message: "Hello from inventory-service"
    contact-details:
      email: "default-admin@mini-library.com"
      name: "Default Admin"
    on-call-support:
      - "123-456-7890"
      - "098-765-4321"
      - "135-792-4680"


